interface:
  display_name: "Build Loop"
  short_description: "Prove one Bead through a tight build loop"
  default_prompt: "Use $build-loop to implement the active Bead with tight executable checks and return a compact evidence bundle to the native Beads workflow."
