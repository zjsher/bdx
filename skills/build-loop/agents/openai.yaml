interface:
  display_name: "Build Loop"
  short_description: "Implement a settled milestone with tight checks"
  default_prompt: "Use $build-loop to implement the current planned milestone, proving each behavior with the tightest executable check and persisting progress at milestone boundaries."
